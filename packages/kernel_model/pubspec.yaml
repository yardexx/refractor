name: kernel_model
description: Pure-Dart model for representing parsed kernel AST trees.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  kernel:
    path: ../../third_party/kernel

dev_dependencies:
  test: ^1.24.0
  very_good_analysis: ^10.1.0